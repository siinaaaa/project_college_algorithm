// script.js
function changeFont(fontName) {
    document.getElementById('inputText').style.fontFamily = fontName;
}

function changeColor(color) {
    document.getElementById('inputText').style.color = color;
}

function translateText(language) {
    // اینجا باید کد برای تبدیل زبان اضافه شود
    // می‌توانید از API‌های ترجمه مانند Google Translate API استفاده کنید
}

function saveText() {
    // اینجا باید کد برای ذخیره متن اضافه شود
}

function checkSpelling() {
    // اینجا باید کد برای بررسی املا اضافه شود
    // می‌توانید از API‌های بررسی املا مانند Grammarly API استفاده کنید
}
